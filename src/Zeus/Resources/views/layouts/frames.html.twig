<div class="olh-media-frame media-frame">
    <div class="media-frame-router">
        <div class="media-router">
            {% for i,item in items %}
                {% set active = i == 1 ? ' active' : '' %}
                <a href="#olh-{{ item.id }}" class="media-menu-item{{ active }}">{{ item.name|raw }}</a>
            {% endfor %}
        </div>
    </div>

    <div class="media-frame-content">
        {% for i,item in items %}
            {% set active = i == 1 ? ' active' : '' %}

            <div id="olh-{{ item.id }}" class="media-menu-content{{ active }}">
                {{ item.contents|raw }}
                <br class="olh-clear"/>
            </div>
        {% endfor %}
    </div>
</div>
