<table class="form-table">
    <tbody>
        {% for field in fields %}
            {% set tpl = '@' ~ field.context ~ '/' ~ field.template %}
            {% include tpl with field.vars %}
        {% endfor %}
    </tbody>
</table>
